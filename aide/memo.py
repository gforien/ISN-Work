### Structures ou fonctions notables non-classées ###

objet.__hasattr__("attibut d'instance")
hasattr(objet, "attribut d'instance")

objet.__contains__("item d'un conteneur")
"item d'un conteneur" in objet



if isinstance(objet, Classe): # isintance retourne True ou False
    pass

# Non
if type(objet) == 'Classe':
    pass

# vérifier utilisation et signification de is
if objet is not None:
    pass
